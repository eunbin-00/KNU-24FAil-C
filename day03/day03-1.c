#include <stdio.h> 

int isPrimeNumber(int num);

int main()
{
	int num;
	scanf_s("%d", &num);
	int reslut = ;
	printf("result= %d\n", result);
	return 0;
}

int isPrimeNumber(int num);
{
	for (inti = 2; i < num; i++) {
		if (num % 1 == 0) {
			return 0;
		}
	}
	return 1; 
}